export enum BrepObjectType {
    POINT = 'point',
    EDGE = 'edge',
    FACE = 'face',
}


export enum PickType {
    FACE = 'face',
    EDGE = 'edge',
    VERTEX = 'vertex',
    ALL = 'all',
}

export enum RenderMode {
    IDLE = 'idle',
    EDIT = 'edit',
}